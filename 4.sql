SELECT
  CLIENTS.ID,
  CLIENTS.NAME,
  ACCOUNTS.SALDO,
  ACCOUNTS.ACC_NUM
FROM
  ACCOUNTS
RIGHT JOIN 
  CLIENTS ON ACCOUNTS.CLIENT_REF=CLIENTS.ID
WHERE
  ACCOUNTS.SALDO >= 0 AND
  ACCOUNTS.NAME LIKE '%ДЕПОЗИТ%';



